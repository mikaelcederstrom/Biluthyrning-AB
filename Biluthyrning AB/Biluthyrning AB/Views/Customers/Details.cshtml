@model Biluthyrning_AB.Models.ViewModels.CustomersDetailsVM
@{
    ViewData["Title"] = "Detail";
}
<partial name="_MenuBarCustomers" />

<div class="container">
    <div class="row" style="background-color: lightgray">
        <div class="col-sm">
            Personnummer
        </div>
        <div class="col-sm">
            Efternamn
        </div>
        <div class="col-sm">
            Förnamn
        </div>
        <div class="col-sm">
            AnvändarID
        </div>
    </div>
    <div class="row" style="background-color: bisque">

        <div class="col-sm">
            @Model.PersonNumber
        </div>
        <div class="col-sm">
            @Model.LastName
        </div>
        <div class="col-sm">
            @Model.FirstName
        </div>
        <div class="col-sm">
            @Model.Id
        </div>

    </div>
    <br />
    <br />
    <br />
    @*<div class="row" style="background-color: white">
            <div class="col-sm">
            </div>
            <div class="col-sm">
            </div>
            <div class="col-sm">
            </div>
            <div class="col-sm">
            </div>
        </div>*@

    <div class="row" style="background-color: lightgray">
        <div class="col-sm">
            OrderID
        </div>
        <div class="col-sm">
            Mätarställning före uthyrning (km)
        </div>
        <div class="col-sm">
            Mätarställning efter uthyrning (km)
        </div>
        <div class="col-sm">
            Hyrd (Datum)
        </div>
        <div class="col-sm">
            Återlämnad (Datum)
        </div>

    </div>
    @for (int i = 0; i < Model.Orders.Length; i++)
    {
        if (i % 2 == 0 && Model.Orders[i].CarReturned == true)
        {
            <div class="row" style="background-color: bisque">

                <div class="col-sm">
                    @Model.Orders[i].Id
                </div>
                <div class="col-sm">
                    @Model.Orders[i].KilometerBeforeRental
                </div>
                <div class="col-sm">
                    @Model.Orders[i].KilometerAfterRental
                </div>
                <div class="col-sm">
                    @Model.Orders[i].RentalDate
                </div>
                <div class="col-sm">
                    @Model.Orders[i].ReturnDate
                </div>
            </div>
        }

        if (i % 2 == 1 && Model.Orders[i].CarReturned == true)
        {
            <div class="row" style="background-color: white">
                <div class="col-sm">
                    @Model.Orders[i].Id
                </div>
                <div class="col-sm">
                    @Model.Orders[i].KilometerBeforeRental
                </div>
                <div class="col-sm">
                    @Model.Orders[i].KilometerAfterRental
                </div>
                <div class="col-sm">
                    @Model.Orders[i].RentalDate
                </div>
                <div class="col-sm">
                    @Model.Orders[i].ReturnDate
                </div>
            </div>
        }

    }
    <br />
    <br />
    <br />

    <div class="row" style="background-color: lightgray">
        <div class="col-sm">
            OrderID
        </div>
        <div class="col-sm">
            Mätarställning före uthyrning (km)
        </div>
        <div class="col-sm">
        </div>
        <div class="col-sm">
            Hyrd (Datum)
        </div>
        <div class="col-sm">
        </div>
        </div>
        @for (int i = 0; i < Model.Orders.Length; i++)
        {
            if (i % 2 == 0 && Model.Orders[i].CarReturned == false)
            {
                <div class="row" style="background-color: bisque">

                    <div class="col-sm">
                        @Model.Orders[i].Id
                    </div>
                    <div class="col-sm">
                        @Model.Orders[i].KilometerBeforeRental
                    </div>
                    <div class="col-sm">
                    </div>
                    <div class="col-sm">
                        @Model.Orders[i].RentalDate
                    </div>
                    <div class="col-sm">
                    </div>
                </div>
            }

            if (i % 2 == 1 && Model.Orders[i].CarReturned == false)
            {
                <div class="row" style="background-color: white">
                    <div class="col-sm">
                        @Model.Orders[i].Id
                    </div>
                    <div class="col-sm">
                        @Model.Orders[i].KilometerBeforeRental
                    </div>
                    <div class="col-sm">
                    </div>
                    <div class="col-sm">
                        @Model.Orders[i].RentalDate
                    </div>
                    <div class="col-sm">
                    </div>
                </div>
            }

        }

    </div>

